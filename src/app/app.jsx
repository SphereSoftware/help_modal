import React, { PureComponent } from 'react';
import css from './style.css';

export default class App extends PureComponent {
  render() {
    return (
      <div className={ css.test }>help modal</div>
    );
  }
}
